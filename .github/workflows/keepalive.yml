name: Keep Supabase Alive

on:
  workflow_dispatch:
  schedule:
    - cron: "0 */12 * * *"

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Supabase
        run: curl https://tuhnauejmcufnfwmvwba.supabase.co
        
